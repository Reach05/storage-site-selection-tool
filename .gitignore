# ── Archives & Zips ──
site-selection-screenshots.zip
site-selection-screenshots-storage.zip

# ── Next.js build output ──
.next/
out/

# ── Node dependencies ──
node_modules/

# ── Local env files ──
.env
.env.local
.env.*.local

# ── Vercel / Cloud Run artifacts ──
site-selection-tool.zip
.vercel
.fake

# ── Screenshots & IDE workspaces ──
screenshots/
*.code-workspace

# ── OS / Editor files ──
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── ArcGIS assets (exclude SASS/SCSS) ──
public/arcgis/assets/**/*.scss
public/arcgis/assets/**/*.sass

# ── Allow our well-known stubs ──
!public/empty.json
!public/.well-known/appspecific/
!public/.well-known/appspecific/com.chrome.devtools.json

# ── Ignore hint config ──
.hintrc

# ── Ensure TS config & Next.js env typings are tracked ──
!tsconfig.json
!next-env.d.ts
!postcss.config.cjs

# ── Track specific components