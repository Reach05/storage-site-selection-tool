# ── Archives & Zips ──
site-selection-screenshots.zip
site-selection-screenshots-storage.zip

# ── Next.js build output ──
.next/
out/

# ── Node dependencies ──
node_modules/

# ── Local env files ──
.env
.env.local
.env.*.local

# ── Vercel / Cloud Run artifacts ──
site-selection-tool.zip

# ── Screenshots & IDE workspaces ──
screenshots/
*.code-workspace

# ── OS / Editor files ──
.DS_Store
Thumbs.db
.idea/
.vscode/

.vercel
.fake

# allow our well-known stub
!public/empty.json
!public/.well-known/appspecific/
!public/.well-known/appspecific/com.chrome.devtools.json

# ignore hint config
.hintrc

# ensure TS config and Next.js env typings are tracked
!tsconfig.json
!next-env.d.tsnpm install
!postcss.config.cjs

# explicitly track LegendPanel
!components/LegendPanel.js