/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // if you are accessing via http://10.211.55.7:3000 in development, add it here:
  allowedDevOrigins: ["http://10.211.55.7:3000"],
};

export default nextConfig;
